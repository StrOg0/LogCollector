<Window x:Class="LogCollectorApp.Windows.GroupEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование группы" Height="250" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название группы:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="0" x:Name="txtName" Margin="0,20,0,15" Padding="5"/>

        <TextBlock Grid.Row="1" Text="Описание:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="txtDescription" Margin="0,20,0,15" Padding="5" AcceptsReturn="True"/>

        <CheckBox Grid.Row="2" x:Name="chkIsActive" Content="Активна" Margin="0,0,0,15" IsChecked="True"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Сохранить" Width="100" Margin="0,0,10,0" Click="BtnSave_Click"/>
            <Button Content="Отмена" Width="100" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>