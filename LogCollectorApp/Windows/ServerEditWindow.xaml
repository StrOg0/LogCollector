<Window x:Class="LogCollectorApp.Windows.ServerEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование сервера" Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название сервера:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="0" x:Name="txtName" Margin="0,20,0,15" Padding="5"/>

        <TextBlock Grid.Row="1" Text="IP-адрес:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" x:Name="txtIpAddress" Margin="0,20,0,15" Padding="5"/>

        <TextBlock Grid.Row="2" Text="SSH порт:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="txtPort" Margin="0,20,0,15" Padding="5" Text="22"/>

        <CheckBox Grid.Row="3" x:Name="chkIsActive" Content="Активен" Margin="0,0,0,15" IsChecked="True"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="100" Margin="0,0,10,0" Click="BtnSave_Click"/>
            <Button Content="Отмена" Width="100" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>